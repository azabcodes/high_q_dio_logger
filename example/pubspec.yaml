name: high_q_dio_logger_example
description: An example project demonstrating the high_q_dio_logger package.
publish_to: 'none'

environment:
  sdk: ^3.12.0
  flutter: ^3.44.0

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  high_q_dio_logger:
    path: ../

dev_dependencies:
  flutter_lints: ^3.0.0
